---
title: UX-Änderungen zwischen WSL 1 und 2-WSL
description: Änderungen des Benutzererlebnisses zwischen WSL 1 und 2-WSL
keywords: BashOnWindows, bash, wsl, wsl2, windows, windows subsystem for linux, windowssubsystem, ubuntu, debian, suse, windows 10
author: mscraigloewen
ms.author: mscraigloewen
ms.date: 05/30/2019
ms.topic: article
ms.assetid: 7afaeacf-435a-4e58-bff0-a9f0d75b8a51
ms.custom: seodec18
ms.openlocfilehash: 0660f9f726811008685de9f1ff457e7515add67a
ms.sourcegitcommit: bb88269eb37405192625fa81ff91162393fb491f
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 06/12/2019
ms.locfileid: "67038100"
---
# <a name="user-experience-changes-between-wsl-1-and-wsl-2"></a><span data-ttu-id="ef5bd-104">Änderungen des Benutzererlebnisses zwischen WSL 1 und 2-WSL</span><span class="sxs-lookup"><span data-stu-id="ef5bd-104">User Experience Changes Between WSL 1 and WSL 2</span></span>

<span data-ttu-id="ef5bd-105">Auf dieser Seite verläuft über die Unterschiede hinsichtlich der benutzerfreundlichkeit zwischen WSL 1 und der WSL-2-Vorschau.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-105">This page goes over the user experience differences between WSL 1 and the WSL 2 preview.</span></span> <span data-ttu-id="ef5bd-106">Die wichtigsten Änderungen, zu berücksichtigen sind:</span><span class="sxs-lookup"><span data-stu-id="ef5bd-106">The key changes to be aware of are:</span></span>

- <span data-ttu-id="ef5bd-107">Speichern Sie Dateien, die Ihre Linux-apps in Ihrem Linux-Root-Dateisystem für schnellere Leistung, Geschwindigkeit Datei zugegriffen wird</span><span class="sxs-lookup"><span data-stu-id="ef5bd-107">Place files that your Linux apps will access in your Linux root file system for faster file performance speed</span></span>
- <span data-ttu-id="ef5bd-108">In ersten die WSL-2-Vorschau-Builds müssen Sie den Zugriff auf netzwerkanwendungen mithilfe einer IP-Adresse und nicht "localhost"</span><span class="sxs-lookup"><span data-stu-id="ef5bd-108">In initial builds of the WSL 2 preview you will need to access network applications using an IP address and not using localhost</span></span>

<span data-ttu-id="ef5bd-109">Und unten ist die vollständige Liste der anderen Änderungen, die Sie ggf. bemerken:</span><span class="sxs-lookup"><span data-stu-id="ef5bd-109">And below is the full list of other changes that you may notice:</span></span>

- <span data-ttu-id="ef5bd-110">WSL 2 verwendet eine virtuelle Festplatte zum Speichern der Dateien, und wenn Sie die maximale Größe erreichen müssen Sie möglicherweise um ihn zu erweitern</span><span class="sxs-lookup"><span data-stu-id="ef5bd-110">WSL 2 uses a VHD to store your files and if you reach its max size you may need to expand it</span></span>
- <span data-ttu-id="ef5bd-111">Beim Start wird WSL 2 einen kleinen Anteil des Speichers jetzt verwenden.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-111">When starting, WSL 2 will now use a small proportion of memory</span></span>
- <span data-ttu-id="ef5bd-112">Cross-Betriebssystem wird zugriffsgeschwindigkeit der Datei in der ersten Vorschau-Builds langsamer sein.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-112">Cross OS file access speed will be slower in initial preview builds</span></span>

## <a name="place-your-linux-files-in-your-linux-root-file-system"></a><span data-ttu-id="ef5bd-113">Speichern Sie die Linux-Dateien in Ihrem Linux-Root-Dateisystem</span><span class="sxs-lookup"><span data-stu-id="ef5bd-113">Place your Linux files in your Linux root file system</span></span>
<span data-ttu-id="ef5bd-114">Stellen Sie sicher, dass die Dateien, die Sie häufig mit Linux zugreifen, stellen Anwendungen innerhalb Ihrer Linux-root-Dateisystem, um die Datei Leistungsvorteile nutzen zu können.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-114">Make sure to put the files that you will be accessing frequently with Linux applications inside of your Linux root file system to enjoy the file performance benefits.</span></span> <span data-ttu-id="ef5bd-115">Diese Dateien müssen in das Stammdateisystem für Linux, damit schnellere Dateisystemzugriff sein.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-115">These files have to be inside of the Linux root file system to have faster file system access.</span></span> <span data-ttu-id="ef5bd-116">Wir haben auch möglich, dass Windows-apps, die Linux-Root-Dateisystem (z. B. Datei-Explorer! den Zugriff auf die vereinfacht</span><span class="sxs-lookup"><span data-stu-id="ef5bd-116">We have also made it possible for Windows apps to access the Linux root file system (like File Explorer!</span></span> <span data-ttu-id="ef5bd-117">Versuchen Sie es ausgeführt wird: `explorer.exe /` in Ihrem Bash-Shell und was geschieht, finden Sie unter) dem erleichtert diesen Übergang erheblich.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-117">Try running: `explorer.exe /` in your bash shell and see what happens) which will make this transition significantly easier.</span></span> 

## <a name="accessing-network-applications"></a><span data-ttu-id="ef5bd-118">Zugriff auf netzwerkanwendungen</span><span class="sxs-lookup"><span data-stu-id="ef5bd-118">Accessing network applications</span></span>
<span data-ttu-id="ef5bd-119">In der ersten die WSL-2-Vorschau-Builds müssen Sie die IP-Adresse Ihrer Linux-Distribution und einem beliebigen Windows-Server unter Linux, die die IP-Adresse des Host-Computers mit Windows auf einem Linux-Server.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-119">In the initial builds of the WSL 2 preview, you will need to access any Linux server from Windows using the IP address of your Linux distro, and any Windows server from Linux using the IP address of your host machine.</span></span> <span data-ttu-id="ef5bd-120">Dies ist etwas, das temporäre, und auf unserer Prioritätenliste beheben sehr hoch ist.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-120">This is something that is temporary, and very high on our priority list to fix.</span></span>

### <a name="accessing-linux-applications-from-windows"></a><span data-ttu-id="ef5bd-121">Zugreifen auf Linux-Anwendungen aus Windows</span><span class="sxs-lookup"><span data-stu-id="ef5bd-121">Accessing Linux applications from Windows</span></span>
<span data-ttu-id="ef5bd-122">Wenn Sie einen Server in einer Distribution WSL verfügen, müssen Sie die IP-Adresse des virtuellen Computers verbessern der Leistung von Ihrer Distribution zu finden und mit dieser IP-Adresse herstellen.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-122">If you have a server in a WSL distro, you'll need to find the IP address of the virtual machine powering your distro and connect to it with that IP address.</span></span> <span data-ttu-id="ef5bd-123">Sie können dazu folgende Schritte:</span><span class="sxs-lookup"><span data-stu-id="ef5bd-123">You can do that by following these steps:</span></span>

- <span data-ttu-id="ef5bd-124">Erhalten Sie durch Ausführen des Befehls die IP-Adresse Ihrer Distribution `ip addr` innerhalb Ihrer Distribution WSL und finden sie unter den `inet` Wert der `eth0` Schnittstelle.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-124">Obtain the IP address of your distro by running the command `ip addr` inside of your WSL distro and finding it under the `inet` value of the `eth0` interface.</span></span>
   - <span data-ttu-id="ef5bd-125">Sie finden diese leichter durch Filtern der Ausgabe des Befehls mit Grep folgendermaßen: `ip addr | grep eth0`.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-125">You can find this more easily by filtering the output of the command using grep like so: `ip addr | grep eth0`.</span></span>
- <span data-ttu-id="ef5bd-126">Verbinden Sie mit dem Linux-Server mit der IP-Adresse, dem Sie weiter oben wurde gefunden.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-126">Connect to your Linux server using the IP you found above.</span></span>

<span data-ttu-id="ef5bd-127">Die folgende Abbildung zeigt ein Beispiel dafür durch Herstellen einer Verbindung mit einem Node.js-Server mit Microsoft Edge-Browser.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-127">The picture below shows an example of this by connecting to a nodeJS server using the Edge browser.</span></span>

![Zugreifen auf Linux-netzwerkanwendungen über Windows](media/wsl2-network-w2l.jpg)

### <a name="accessing-windows-applications-from-linux"></a><span data-ttu-id="ef5bd-129">Zugriff auf Windows-Anwendungen unter Linux</span><span class="sxs-lookup"><span data-stu-id="ef5bd-129">Accessing Windows applications from Linux</span></span>
<span data-ttu-id="ef5bd-130">Für den Zugriff auf eine Windows-Netzwerk-Anwendung müssen Sie die IP-Adresse des Host-Computers zu verwenden.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-130">To access a Windows network application you'll need to use the IP address of your host machine.</span></span> <span data-ttu-id="ef5bd-131">Mit diesen Schritten können Sie tun:</span><span class="sxs-lookup"><span data-stu-id="ef5bd-131">You can do so with these steps:</span></span>

- <span data-ttu-id="ef5bd-132">Abrufen die IP-Adresse des Host-Computers mithilfe des Befehls `cat /etc/resolv.conf` und kopieren die IP-Adresse, die nach dem Begriff `nameserver`.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-132">Obtain the IP address of your host machine by running the command `cat /etc/resolv.conf` and copying the IP address following the term `nameserver`.</span></span> 
- <span data-ttu-id="ef5bd-133">Verbinden Sie mit einem beliebigen Windows-Server, der die kopierten IP-Adresse.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-133">Connect to any Windows server using the copied IP address.</span></span>

<span data-ttu-id="ef5bd-134">Die folgende Abbildung zeigt ein Beispiel dafür durch das Verbinden mit einem Python-Server unter Windows über Curl.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-134">The picture below shows an example of this by connecting to a python server running in Windows via curl.</span></span> 

![Zugreifen auf Linux-netzwerkanwendungen über Windows](media/wsl2-network-l2w.png)

## <a name="understanding-wsl-2-uses-a-vhd-and-what-to-do-if-you-reach-its-max-size"></a><span data-ttu-id="ef5bd-136">Grundlegendes zu WSL 2 verwendet, eine virtuelle Festplatte, und was Sie tun, wenn Sie die maximale Größe erreicht</span><span class="sxs-lookup"><span data-stu-id="ef5bd-136">Understanding WSL 2 uses a VHD, and what to do if you reach its max size</span></span>
<span data-ttu-id="ef5bd-137">WSL-2 speichert alle Linux-Dateien in eine VHD, die das ext4-Dateisystem verwendet.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-137">WSL 2 stores all your Linux files inside of a VHD that uses the ext4 file system.</span></span> <span data-ttu-id="ef5bd-138">Diese virtuelle Festplatte wird automatisch um die speicheranforderungen zu erfüllen.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-138">This VHD automatically resizes to meet your storage needs.</span></span> <span data-ttu-id="ef5bd-139">Diese virtuelle Festplatte verfügt auch über eine maximale anfängliche Größe von 256GB.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-139">This VHD also has an initial max size of 256GB.</span></span> <span data-ttu-id="ef5bd-140">Wenn Ihre Distribution in Größe größer sein als 256 GB vergrößert wird, sehen Sie Fehler, die besagt, dass Sie, nicht genügend Speicherplatz vorhanden ausgeführt haben.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-140">If your distro grows in size to be greater than 256GB you will see errors stating that you've run out of disk space.</span></span> <span data-ttu-id="ef5bd-141">Sie können diese durch Erweitern der VHD-Größe beheben.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-141">You can fix these by expanding the VHD size.</span></span> <span data-ttu-id="ef5bd-142">Anweisungen dazu finden Sie unten:</span><span class="sxs-lookup"><span data-stu-id="ef5bd-142">Instructions on how to do so are below:</span></span>

1. <span data-ttu-id="ef5bd-143">Beenden Sie alle WSL-Instanzen, die mit der die `wsl --shutdown` Befehl</span><span class="sxs-lookup"><span data-stu-id="ef5bd-143">Terminate all WSL instances using the `wsl --shutdown` command</span></span>
2. <span data-ttu-id="ef5bd-144">Ändern der Größe der virtuellen Festplatte WSL-2 dazu die folgenden Befehle</span><span class="sxs-lookup"><span data-stu-id="ef5bd-144">Resize your WSL 2 VHD by completing the following commands</span></span>
   - <span data-ttu-id="ef5bd-145">Öffnen Sie ein Eingabeaufforderungsfenster mit Administratorrechten, und führen Sie die folgenden Befehle aus:</span><span class="sxs-lookup"><span data-stu-id="ef5bd-145">Open a command prompt Window with admin privileges and run the following commands:</span></span>
      - `Select vdisk file="<pathToVHD>"`
      - `expand vdisk maximum="<sizeInMegaBytes>"`
3. <span data-ttu-id="ef5bd-146">Starten Sie Ihre Distribution WSL</span><span class="sxs-lookup"><span data-stu-id="ef5bd-146">Launch your WSL distro</span></span>
4. <span data-ttu-id="ef5bd-147">Stellen Sie die WSL Beachten Sie, dass das Dateisystem Größe erweitert werden kann</span><span class="sxs-lookup"><span data-stu-id="ef5bd-147">Make WSL aware that it can expand its file system's size</span></span>
   - <span data-ttu-id="ef5bd-148">Führen Sie diese Befehle in Ihrer Distribution WSL:</span><span class="sxs-lookup"><span data-stu-id="ef5bd-148">Run these commands in your WSL distro:</span></span>
      - `sudo mount -t devtmpfs none /dev`
      - `mount | grep ext4`
         - <span data-ttu-id="ef5bd-149">Kopieren Sie den Namen dieses Eintrags, der aussieht: / Dev/SdXX (mit dem X, die alle anderen Zeichen darstellt.)</span><span class="sxs-lookup"><span data-stu-id="ef5bd-149">Copy the name of this entry, which will look like: /dev/sdXX (with the X representing any other character)</span></span>
      - `sudo resize2fs /dev/sdXX`
         - <span data-ttu-id="ef5bd-150">Stellen Sie sicher, den Wert Sie zuvor kopiert haben, und Sie verwenden möchten: `apt install resize2fs`.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-150">Make sure to use the value you copied earlier, and you may need to use: `apt install resize2fs`.</span></span>

## <a name="wsl-2-will-use-some-memory-on-startup"></a><span data-ttu-id="ef5bd-151">WSL 2 wird Arbeitsspeicher beim Start verwenden.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-151">WSL 2 will use some memory on startup</span></span>
<span data-ttu-id="ef5bd-152">WSL 2 verwendet ein einfaches Hilfsprogramm-VM auf einem echten Linux-Kernel, um zur Verfügung zu stellen hervorragende Leistung des Dateisystems und vollständige Kompatibilität und dennoch wird ebenso wie Licht, schnelle, integrierte und reaktionsfähig wie WSL 1 aufrufen.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-152">WSL 2 uses a lightweight utility VM on a real Linux kernel to provide great file system performance and full system call compatibility while still being just as light, fast, integrated and responsive as WSL 1.</span></span> <span data-ttu-id="ef5bd-153">Dieses Hilfsprogramm-VM einen kleinen Speicherbedarf aufweist und belegt Arbeitsspeicher beim Start virtuelle Adresse, die gesichert.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-153">This utility VM has a small memory footprint and will allocate Virtual Address backed memory on startup.</span></span> <span data-ttu-id="ef5bd-154">Starten Sie einen kleinen Anteil des Ihrer gesamten Arbeitsspeichers konfiguriert ist.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-154">It is configured to start with a small proportion of your total memory.</span></span>

## <a name="cross-os-file-speed-will-be-slower-in-initial-preview-builds"></a><span data-ttu-id="ef5bd-155">Cross-Betriebssystem wird Datei Geschwindigkeit in der ersten Vorschau-Builds langsamer sein.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-155">Cross OS file speed will be slower in initial preview builds</span></span>
<span data-ttu-id="ef5bd-156">Sie werden feststellen, langsameren Datei WSL 1 verglichen wird, wenn der Zugriff auf Windows-Dateien auf einer Linux-Anwendung und den Zugriff auf Linux-Dateien aus einer Windows-Anwendung.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-156">You will notice slower file speeds compared to WSL 1 when accessing Windows files from a Linux application, or accessing Linux files from a Windows application.</span></span> <span data-ttu-id="ef5bd-157">Dies ist das Ergebnis der Änderungen an der Architektur in WSL 2 und etwas, das die WSL-Team aktiv wird untersucht, wie wir diese Erfahrung verbessern können.</span><span class="sxs-lookup"><span data-stu-id="ef5bd-157">This is a result of the architectural changes in WSL 2, and is something that the WSL team is actively investigating on how we can improve this experience.</span></span>